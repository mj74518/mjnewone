import { CalDiscountPipe } from './cal-discount.pipe';

describe('CalDiscountPipe', () => {
  it('create an instance', () => {
    const pipe = new CalDiscountPipe();
    expect(pipe).toBeTruthy();
  });
});
